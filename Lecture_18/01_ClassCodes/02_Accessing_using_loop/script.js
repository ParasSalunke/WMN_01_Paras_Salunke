//	Declaration of Array

// let arr = new Array();
// let arr = []

let colors = ["Red", "Green", "Blue", "Cyan", "Magenta", "Yellow", "White", "Black"];

//	Array element access

//	Access Array elements using Loop
for (let le = 0; le < colors.length; ++le) {
	alert(colors[le]);
}